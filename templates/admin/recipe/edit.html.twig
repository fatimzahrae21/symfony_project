{% extends 'admin/admin.html.twig'%}

{% block title 'Recette : ' ~ recipe.title %}

{% block body%}
 

  <h1>{{ recipe.title}} </h1>
<p>
  <img src="/recettes/images/{{ recipe.thumbnail }}" >
</p>

 {{  form_start(form) }}

 <div class="d-flex">
 {{ form_row(form.title)}}
  {{ form_row(form.duration)}}

 </div>
  {{  form_end(form) }}
{% endblock %}